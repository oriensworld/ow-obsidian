---
description: Inspect, configure, or continue the current Obsidian knowledge vault.
argument-hint: [vault path or request]
---

Use the `ow-obsidian` skill. Treat `$ARGUMENTS` as an optional explicit vault path or task. Resolve the consuming vault, read its local instructions, report the discovered vault contract, and perform the requested setup or status workflow.
