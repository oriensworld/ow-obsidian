---
description: Preview or create an extractive rollup of vault log entries.
argument-hint: [dry-run|commit] [count or k]
---

Use the `ow-obsidian-fold` skill for `$ARGUMENTS`. Default to dry-run and require explicit authorization before writing.
