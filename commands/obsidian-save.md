---
description: Save a durable insight or conversation as a linked Obsidian note.
argument-hint: [title or destination]
---

Use the `ow-obsidian-save` skill. Treat `$ARGUMENTS` as an optional title or destination and preserve the current conversation's durable knowledge in the consuming vault.
