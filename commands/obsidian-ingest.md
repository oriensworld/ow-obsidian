---
description: Ingest immutable sources into derived linked Obsidian notes.
argument-hint: <source path, URL, or description>
---

Use the `ow-obsidian-ingest` skill to process `$ARGUMENTS` in the current consuming vault. Preserve source files and follow the vault's local organization and note conventions.
